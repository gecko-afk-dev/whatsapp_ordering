# Replace this domain with your actual server domain/subdomain
# e.g., app.myrestaurant-platform.com
yourdomain.com {
    # Route all traffic to the FastAPI app container on port 8000
    reverse_proxy app:8000
}